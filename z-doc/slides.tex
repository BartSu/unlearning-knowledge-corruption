% !TEX program = xelatex
%
% Build:
%   xelatex slides.tex
%   xelatex slides.tex
%
% Requires: beamer, metropolis theme, ctex (Chinese), booktabs, tikz.
% Install hints:
%   sudo apt install texlive-xetex texlive-lang-chinese texlive-fonts-extra
% Or use Overleaf with compiler = XeLaTeX.

\documentclass[aspectratio=169,10pt]{beamer}

\usetheme[progressbar=frametitle,numbering=fraction]{metropolis}
\usepackage{ctex}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds}
\usepackage{graphicx}
\graphicspath{{figures/}}

\definecolor{mDarkTeal}{HTML}{23373B}
\definecolor{mLightBrown}{HTML}{EB811B}
\definecolor{mLightGreen}{HTML}{14B03D}
\setbeamercolor{alerted text}{fg=mLightBrown}

\title{Three-Layer View of Knowledge Corruption\\ in LLM Unlearning}
\subtitle{为什么要\emph{审计} forget set，而不是每次都 unlearn}
\date{}
\author{Bo \;|\; \texttt{unlearning/}}
\institute{Llama-3.1-8B-Instruct \;+\; GradAscent \;+\; WikiText-103 (HDBSCAN × 10)}

\begin{document}

\maketitle

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}{一句话贡献}
    \begin{center}
        \Large
        Unlearning 的副作用沿语义距离\textbf{三层衰减，但不归零}。
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \Large
        \textbf{L1 \;1.76×} \;→\; \textbf{L2 \;1.29×} \;→\; \textbf{L3 \;1.16×}
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \normalsize
        且 \alert{61.6\,\%} 的「无关」样本 PPL 也被抬高。
    \end{center}

    \vfill
    \footnotesize\itshape\centering
    Llama-3.1-8B-Instruct · GradAscent · $n=10$ forget sets.
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{动机与定义}

\begin{frame}{动机：一句话}
    \vfill
    \begin{center}
        \Large
        换个 forget set，副作用能差 \textbf{1.7×}。
    \end{center}

    \vspace{0.6em}
    \begin{center}
        \normalsize
        可每跑一个 unlearn 要数 GPU-hour。
    \end{center}

    \vspace{1.0em}
    \begin{center}
        \Large
        \alert{能不能不 fine-tune，就预测副作用？}
    \end{center}
    \vfill
\end{frame}

\begin{frame}{三层是什么：一张同心圆图}
    \centering
    \begin{tikzpicture}[font=\small]
        \fill[mLightBrown!55]  (0,0) circle (1.1);
        \fill[mLightBrown!25]  (0,0) circle (2.1) (0,0) circle (1.1);
        \draw[thick] (0,0) circle (1.1);
        \draw[thick] (0,0) circle (2.1);
        \draw[thick, dashed] (0,0) circle (3.3);

        \node at (0,0)       {\textbf{L1}};
        \node at (0,1.55)    {\textbf{L2}};
        \node at (0,2.7)     {\textbf{L3}};

        \node[right=3mm] at (1.1,0)  {\footnotesize 被 unlearn 的那批样本（同一 cluster · train）};
        \node[right=3mm] at (2.1,1.55) {\footnotesize 同 cluster 的\emph{没训过}的样本（val/test）};
        \node[right=3mm] at (3.3,2.7)  {\footnotesize \emph{别的 cluster} 的样本};
    \end{tikzpicture}

    \vspace{0.6em}
    \centering
    指标：
    $\displaystyle r = \frac{\text{PPL}_{\text{unlearned}}}{\text{PPL}_{\text{base}}}$
    \quad（PPL 被抬高几倍）

    \vspace{0.6em}
    \footnotesize
    L1 = 想遗忘的\quad L2 = 连带忘的\quad L3 = 不该忘的也被波及
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}{Measurement Pipeline}
    \centering
    \begin{tikzpicture}[
        node distance=3.5mm,
        box/.style={draw, rounded corners=2pt, align=center,
                    minimum height=8mm, text width=28mm, font=\scriptsize},
        sm/.style={draw, rounded corners=2pt, align=center,
                   minimum height=6mm, text width=20mm, font=\scriptsize},
        arr/.style={-Latex, thick, mDarkTeal},
    ]
        \node[box, fill=mDarkTeal!12] (c) {WikiText-103\\HDBSCAN $\to$ \textbf{10 语义簇}};
        \node[box, fill=mDarkTeal!12, right=6mm of c] (t) {每簇采 1 triplet\\= 3 splits $\times$ 50 文本};
        \node[box, fill=mLightGreen!20, right=6mm of t] (u) {\textbf{10 个 GradAscent}\\unlearn ckpt};
        \node[box, fill=mLightBrown!25, right=6mm of u] (m) {\textbf{10$\times$10 cross-matrix}\\per-sample ppl};

        \draw[arr] (c) -- (t);
        \draw[arr] (t) -- (u);
        \draw[arr] (u) -- (m);
    \end{tikzpicture}

    \vspace{1.0em}
    \scriptsize
    \begin{tabular}{@{}lll@{}}
        \toprule
        矩阵位置 & 分片 & 用作 \\
        \midrule
        diagonal \;\; $m = e$ & \texttt{train} (50)               & \textbf{L1} forget \; ($n=500$) \\
        diagonal \;\; $m = e$ & \texttt{validation + test} (100)  & \textbf{L2} locality \; ($n=1\,000$) \\
        off-diagonal \;\; $m \neq e$ & \texttt{test} (50)                & \textbf{L3} spillover \; ($n=4\,500$) \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \footnotesize
    Triplet $=$ \texttt{(train, validation, test)} 三分片均来自\emph{同一}
    语义簇、\emph{互不重叠}；每分片 50 文本。监督信号 $=\log r$。
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{第一幕 · 数据证实三层 View}

\begin{frame}{这一幕要说服你三件事}
    \vspace{0.8em}
    \begin{description}
        \setlength\itemsep{0.8em}
        \item[\alert{C1}] 三层\textbf{严格递减}（L1 > L2 > L3），不是乱分箱。
        \item[\alert{C2}] L3 \textbf{不是 0}：无关样本也真被波及。
        \item[\alert{C3}] profile \textbf{随 forget set 变化}，所以\emph{值得审计}。
    \end{description}
\end{frame}

\begin{frame}{C1 + C2：层级 headline（全池样本）}
    \centering
    \begin{tabular}{@{}lrrrr@{}}
        \toprule
        Layer & $n$ & geo-mean $r$ & $>1.1\times$ & $>2\times$ \\
        \midrule
        L1 forget     &   500 & \textbf{1.762$\times$} & 95.6\,\% & 33.2\,\% \\
        L2 locality   & 1\,000 & \textbf{1.290$\times$} & 82.8\,\% &  3.4\,\% \\
        L3 spillover  & 4\,500 & \textbf{1.158$\times$} & \alert{61.6\,\%} &  0.0\,\% \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \begin{itemize}
        \item \textbf{C1 成立}：1.762 → 1.290 → 1.158 严格单调；L2 保留 L1
              损伤 \textbf{38\,\%}，L3 保留 \textbf{21\,\%}。
        \item \textbf{C2 成立}：L3 geo-mean $=1.158\times$、\alert{61.6\,\%}
              跨簇样本 surprisal 真的上升 $\Rightarrow$ 溢出\emph{不可忽略}。
        \item 消歧参照：self (同簇 test) $1.291\times$ vs. cross $1.158\times$，
              locality multiplier 仅 \textbf{1.114$\times$} —— L2 只比 L3 高
              $\sim$11\,\%，\alert{局部性存在但比常见直觉弱}。
    \end{itemize}
\end{frame}

\begin{frame}{C1 + C2：三层衰减可视化}
    \centering
    \includegraphics[width=0.82\textwidth]{fig_three_layer_decay.pdf}

    \vspace{0.4em}
    \footnotesize
    左轴：几何均值 $r$ 随层级严格递减（\textbf{C1}）；
    右轴：$r>1.1$ 的样本比例在 L3 仍达 \alert{61.6\,\%}（\textbf{C2}：spillover 非平凡）。
\end{frame}

\begin{frame}{C3：三层 profile 随 forget set 而变（差 1.7$\times$）}
    \centering
    \scriptsize
    \begin{tabular}{@{}lrrrrr@{}}
        \toprule
        forget set & 簇 & L1 & L2 & L3\_out & \\
        \midrule
        triplet\_091 & star    & 1.42 & 1.12 & 1.07 & \footnotesize 最温和 \\
        triplet\_001 & game    & 1.44 & 1.17 & 1.07 & \\
        triplet\_011 & federer & 1.45 & 1.11 & 1.07 & \\
        triplet\_021 & jordan  & 1.71 & 1.31 & 1.20 & \\
        triplet\_081 & river   & 1.73 & 1.23 & 1.18 & \\
        triplet\_061 & war     & 1.81 & 1.21 & 1.18 & \\
        triplet\_051 & song    & 1.86 & 1.29 & 1.19 & \\
        triplet\_041 & league  & 1.99 & 1.42 & 1.20 & \\
        triplet\_031 & episode & 2.03 & 1.39 & 1.19 & \\
        \alert{triplet\_071} & \alert{storm}   & \alert{\textbf{2.44}} & \alert{\textbf{1.78}} & \alert{\textbf{1.25}} & \footnotesize 最严重 \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \begin{itemize}\small
        \item \textbf{C3 成立}：L1 跨 forget set 差 \textbf{1.72$\times$}、
              L2 差 1.60$\times$、L3\_out 差 1.17$\times$ —— profile 是
              forget set 的函数，不是算法常数。
        \item 嵌入紧凑度（低 emb variance）与 L2 的 Spearman $\rho=0.50$
              ($n=10$，CI 较宽，提示性证据）—— 已暗示\emph{几何可预测
              corruption}（第二幕将正式化）。
        \item 方法学含义：\alert{仅用单 forget set 给 unlearner 打分会显著
              低估评分方差} $\Rightarrow$ suggests benchmark instability，
              而非"系统性误报"。
    \end{itemize}

    \vspace{0.4em}
    \centering\footnotesize
    \emph{小结}：C1 + C2 说明三层是\emph{有序且 L3 非平凡}的结构；C3 说明
    profile 因 forget set 而异 $\Rightarrow$ \textbf{预测 profile 是良定义
    问题}（$\to$ 第二幕）。
\end{frame}

\begin{frame}{C3 可视化：10 个 forget set 的 profile}
    \centering
    \includegraphics[width=0.88\textwidth]{fig_per_forget_profile.pdf}

    \vspace{0.3em}
    \footnotesize
    按 L1 升序排列；\alert{storm} 在三层上同时为最严重 forget set。
    三层的极差比 $=$ L1 1.72$\times$ / L2 1.60$\times$ / L3 1.17$\times$。
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{第二幕 · 与其每次 unlearn，不如审计}

\begin{frame}{审计问题：输入是什么、输出是什么}
    \vspace{0.4em}
    \begin{center}
        \begin{tikzpicture}[font=\small, node distance=10mm]
            \node[draw, rounded corners, fill=mDarkTeal!12, minimum width=32mm, minimum height=12mm, align=center] (in)
                {\textbf{输入}\\ forget set 的\\ 12 维几何特征};
            \node[draw, rounded corners, fill=mLightGreen!25, right=16mm of in, minimum width=28mm, minimum height=12mm, align=center] (box) {\textbf{审计器}\\ Ridge};
            \node[draw, rounded corners, fill=mLightBrown!25, right=16mm of box, minimum width=32mm, minimum height=12mm, align=center] (out)
                {\textbf{输出}\\ L1 / L2 / L3\\ 三个风险分数};
            \draw[-Latex, thick] (in) -- (box);
            \draw[-Latex, thick] (box) -- (out);
        \end{tikzpicture}
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \large
        \alert{不看 evaluation · 不跑 unlearn}
    \end{center}

    \vspace{0.6em}
    \footnotesize\centering
    12 维 = 方差 / 两两相似度 / centroid / 集中度（effective rank, isotropy）。\\
    10 forget set 上 Leave-One-Out。
\end{frame}

\begin{frame}{审计结果：回归 + 排序}
    \centering
    \textbf{回归（LOO over 10 forget sets）}\\[0.3em]
    \begin{tabular}{@{}lrrr@{}}
        \toprule
        审计目标 & $R^2$ & Spearman $\rho$ & Pearson $r$ \\
        \midrule
        L1 forget     & \textbf{+0.443} & +0.624 & +0.684 \\
        L2 locality   & \textbf{+0.410} & +0.624 & +0.658 \\
        L3 spillover  & +0.190          & \textbf{+0.612} & +0.512 \\
        \bottomrule
    \end{tabular}

    \vspace{1.0em}
    \textbf{排序（predicted rank vs. true rank）}\\[0.3em]
    \begin{tabular}{@{}lccc@{}}
        \toprule
        Layer & Spearman $\rho$ & Top-1 命中 & Top-3 重合 \\
        \midrule
        L1 forget     & +0.624 & \alert{\checkmark storm} & 2\,/\,3 \\
        L2 locality   & +0.624 & $\times$ & 2\,/\,3 \\
        L3 spillover  & +0.612 & $\times$ & 1\,/\,3 \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \footnotesize
    L1 层 top-1 与真实排序一致（triplet\_071 storm，\emph{单观察点}，$n=10$）。
    初步证据\emph{支持}审计器作为 \alert{排序 / 筛选工具} 的可行性；是否对更大
    $n$、其他 unlearner、其他 base model 稳健，\emph{尚需验证}。
\end{frame}

\begin{frame}{审计预测散点（LOO, Ridge, 12 维几何）}
    \centering
    \includegraphics[width=0.96\textwidth]{fig_audit_scatter.pdf}

    \vspace{0.2em}
    \footnotesize
    虚线为 $y=x$；三层均取得一致的 $\rho \approx 0.62$ 排序相关；L3 绝对
    幅度小 $\Rightarrow$ $R^2$ 偏低，但\emph{相对排序}依然可用。
\end{frame}

\begin{frame}{Per-sample 对照：几何 $\gg$ 文本表面}
    \centering
    \begin{tabular}{@{}lrr@{}}
        \toprule
        设定 & 特征数 & LOGO $R^2$ \\
        \midrule
        旧：261 维表格（含 per-text 表面统计）   & 261 & +0.142 \\
        \textbf{新：16 维纯几何（forget + target↔forget）} & \textbf{16} & \textbf{+0.362} (GBM) \\
        仅限 L3 跨簇样本（纯 spillover）         & 16  & +0.247 (RF) \\
        \bottomrule
    \end{tabular}

    \vspace{1.0em}
    \begin{block}{解读（谨慎）}
        在当前设置下，几何特征以约 \alert{6\,\%} 的特征数带来 \alert{2.5$\times$}
        的 $R^2$。这\emph{支持} "corruption 与 forget-set 几何比与文本表面
        统计更相关" 的假设；在单 base model、单 unlearner、$n=10$ 下，尚不
        足以断言 "corruption 是几何现象而非文本难度现象"。
    \end{block}
\end{frame}

\begin{frame}{诚实负结果：Naive retain-coverage 不 work}
    把 forget set 的 p95 内部 euclidean 当"高风险半径"，测 retain 落入比例：

    \vspace{0.5em}
    \begin{itemize}
        \item 9\,/\,10 个 forget set 的 mean coverage $\approx 1.0$ —— 半径太大，
              \emph{区分度归零}。
        \item 与真实 L3 spillover 的相关：$\rho = \alert{-0.42}$（方向反了）。
    \end{itemize}

    \vspace{0.4em}
    \textbf{原因}：HDBSCAN 簇在 384 维原空间里互相重叠，p95 半径 > 簇间距。

    \vspace{0.6em}
    \textbf{改进方向}
    \begin{enumerate}
        \item 到 centroid 的\emph{带符号投影}，单尾切片而非球覆盖；
        \item HDBSCAN 原生 mutual-reachability 距离；
        \item 直接把 L3 predictor 当 calibrated per-text risk score。
    \end{enumerate}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{讨论}

\begin{frame}{Related Work（占位，待程序化补全）}
    \begin{description}
        \item[Data-first 鲁棒性预测] Dang et al.\ (ACL 2024): 13 维训练集特征
            $\to$ 对抗 ASR。我们把方法论迁到 \emph{生成式 unlearning 的
            corruption profile 审计}。
        \item[LLM unlearning] GradAscent / NPO / GradDiff / TOFU 由
            \texttt{open-unlearning} 提供；我们\emph{固定 unlearner}，分析
            其 data-conditioned 副作用。
        \item[Influence / editing locality] Model-centric 的局部性已被广研；
            我们用 10$\times$10 cross-ppl 矩阵把它变成可度量的三层风险。
    \end{description}
\end{frame}

\begin{frame}{Limitations}
    \begin{itemize}
        \item \textbf{$n=10$ clusters} —— 审计 $\rho=0.62$ 置信区间较宽；
              100 triplet 全跑可升级到 $n=100$。
        \item \textbf{单 unlearner} —— L1/L2/L3 对 NPO / GradDiff 的稳健性
              待测。
        \item \textbf{单 base model} —— Llama-3.1-8B-Instruct；其他规模 / 家族
              未测。
        \item \textbf{ppl $\neq$ fact} —— L3 的 61.6\,\% 样本是否对应 QA 准确率
              下降？\texttt{2.extract-qa/} 管线已就位。
        \item \textbf{Naive coverage 失败} —— §5.6 需要更细的几何风险度量。
    \end{itemize}
\end{frame}

\begin{frame}{Future Work}
    \begin{enumerate}
        \item \textbf{Scale-up}：100 triplet 全跑 unlearn $\Rightarrow$ $n=100$。
        \item \textbf{跨 unlearner}：加 NPO / GradDiff，审计器须稳健。
        \item \textbf{更好的 coverage}：方向投影 / mutual-reachability。
        \item \textbf{Audit-driven forget 选择}：在 retain 约束下挑
              predicted L3 最小的 $\mathcal{D}_f$，真跑 unlearn 验证。
        \item \textbf{QA 层面}：target 换成 QA accuracy。
    \end{enumerate}
\end{frame}

\begin{frame}{论文定位}
    \begin{itemize}
        \item \textbf{Title（工作名）}：\emph{A Three-Layer View of Knowledge
              Corruption in LLM Unlearning, and Why You Should Audit Before
              You Forget.}
        \item \textbf{Venue}：COLM 2025 / ACL short / EMNLP findings ——
              empirical + analysis + methodological 三位一体。
        \item \textbf{卖点（以当前证据强度措辞）}：
              (1) 三层 view $+$ 61.6\,\% 的 L3 spillover 提示\emph{spillover
                  不可忽略}；
              (2) 把 unlearning benchmarking 重构为 \emph{forget-set audit}；
              (3) 仅 forget-set 几何即可做排序级风险预测（LOO $\rho\approx 0.62$，
                  L1 top-1 匹配为单观察点）。
        \item \textbf{升档}：补到 $n=100$ + 第二个 unlearner
              $\Rightarrow$ NeurIPS / ICML。
    \end{itemize}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}[standout]
    三层衰减 \;·\; spillover 不可忽略 \;·\; 无需 unlearn 的审计 \\[0.4em]
    \normalsize
    \textbf{1.76$\times$ → 1.29$\times$ → 1.16$\times$}\; |\; 61.6\,\%
    \; |\; LOO $\rho \approx 0.62$ \; |\; L1 top-1 吻合 (单点) \\[0.3em]
    \scriptsize\itshape
    单 base model · 单 unlearner · $n=10$ forget sets \;—\; 待更大 $n$ 复证。
\end{frame}

\appendix

\begin{frame}[fragile]{附录 — 复现主结果}
    \footnotesize
\begin{verbatim}
# 0. Unlearn 与 cross-ppl（已完成可跳过）
#    ⇒ 2.extract-ppl/wikitext_cross_metrics_detail.json

# 1. 第一幕：三层 ground truth
cd 2.extract-ppl
python analyze_corruption.py          # ⇒ corruption_summary.json

# 2. 第二幕 A：per-sample 几何预测器
cd ../4.regression-predictor
python 3.corruption_from_geometry.py  # ⇒ geometry/geometry_results.json

# 3. 第二幕 B：forget-set 审计（LOO, Part 1+2+3+3b）
python 4.audit_experiments.py         # ⇒ audit/audit_summary.json
\end{verbatim}
\end{frame}

\begin{frame}{附录 — 关键 artefact}
    \footnotesize
    \begin{tabular}{@{}ll@{}}
        \toprule
        Artefact & 路径 \\
        \midrule
        Cross-eval ppl 明细 & \texttt{2.extract-ppl/wikitext\_cross\_metrics\_detail.json} \\
        三层 corruption 汇总 & \texttt{2.extract-ppl/corruption\_summary.json} \\
        几何 per-sample 预测 & \texttt{4.regression-predictor/geometry/geometry\_results.json} \\
        审计器 LOO 预测 & \texttt{4.regression-predictor/audit/audit\_summary.json} \\
        每 forget set L1/L2/L3 & \texttt{audit/part1\_corruption\_profile.csv} \\
        审计特征表 & \texttt{audit/part2\_forget\_features.csv} \\
        \bottomrule
    \end{tabular}
\end{frame}

\end{document}
