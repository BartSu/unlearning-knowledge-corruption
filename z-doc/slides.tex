% !TEX program = xelatex
%
% Build:
%   xelatex slides.tex
%   xelatex slides.tex
%
% Requires: beamer, metropolis theme, ctex (Chinese), booktabs, tikz.
% Install hints:
%   sudo apt install texlive-xetex texlive-lang-chinese texlive-fonts-extra
% Or use Overleaf with compiler = XeLaTeX.

\documentclass[aspectratio=169,10pt]{beamer}

\usetheme[progressbar=frametitle,numbering=fraction]{metropolis}
\usepackage{ctex}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds}
\usepackage{graphicx}
\graphicspath{{figures/}}

\definecolor{mDarkTeal}{HTML}{23373B}
\definecolor{mLightBrown}{HTML}{EB811B}
\definecolor{mLightGreen}{HTML}{14B03D}
\setbeamercolor{alerted text}{fg=mLightBrown}

\title{From Three-Layer Corruption to Forget-Set Audit\\ in LLM Unlearning}
\subtitle{不跑 unlearn，只看 forget set 几何 $\Rightarrow$ 排序级副作用预警}
\date{}
\author{Bo \;|\; \texttt{unlearning/}}
\institute{Llama-3.1-8B-Instruct \;+\; GradAscent \;+\; WikiText-103 (HDBSCAN × 10)}

\begin{document}

\maketitle

% ────────────────────────────────────────────────────────────────────────────
\section{定义}

\begin{frame}{三层定义 + 量化指标}
    \begin{columns}[T,onlytextwidth]
    \column{0.56\textwidth}
    \centering
    \begin{tikzpicture}[font=\footnotesize]
        \fill[mDarkTeal!10]    (0,0) circle (2.6);
        \fill[mLightBrown!30]  (0,0) circle (1.7);
        \fill[mLightBrown!60]  (0,0) circle (0.85);
        \draw[thick] (0,0) circle (0.85);
        \draw[thick] (0,0) circle (1.7);
        \draw[thick, dashed] (0,0) circle (2.6);

        \node at (0, 0.0)  {\textbf{L1}};
        \node at (0, 1.25) {\textbf{L2}};
        \node at (0, 2.15) {\textbf{L3}};
    \end{tikzpicture}

    \column{0.44\textwidth}
    \small
    \textbf{L1} 想遗忘的\\ \footnotesize 被 unlearn 那批样本

    \vspace{0.4em}
    \small
    \textbf{L2} 连带忘的\\ \footnotesize 同 cluster · 没训过

    \vspace{0.4em}
    \small
    \textbf{L3} 不该忘却被波及\\ \footnotesize 别的 cluster

    \vspace{0.8em}
    \hrule

    \vspace{0.6em}
    \centering
    $r = \dfrac{\text{PPL}_\text{unlearned}}{\text{PPL}_\text{base}}$

    \vspace{0.4em}
    \footnotesize
    每层取几何均值\\ $\Rightarrow$ L1 / L2 / L3 各一个数
    \end{columns}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}{一句话贡献}
    \begin{center}
        \large
        Unlearning 的副作用沿语义距离\textbf{三层衰减，但不归零}。
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \Large
        \textbf{L1 \;1.76×} \;→\; \textbf{L2 \;1.29×} \;→\; \textbf{L3 \;1.16×}
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \normalsize
        且 \alert{61.6\,\%} 的「无关」样本 PPL 也被抬高。
    \end{center}

    \vfill
    \footnotesize\itshape\centering
    Llama-3.1-8B-Instruct · GradAscent · $n=10$ forget sets.
\end{frame}

\begin{frame}{动机：一句话}
    \vfill
    \begin{center}
        \Large
        换个 forget set，副作用能差 \textbf{1.7×}。
    \end{center}

    \vspace{0.6em}
    \begin{center}
        \normalsize
        可每跑一个 unlearn 要数 GPU-hour。
    \end{center}

    \vspace{1.0em}
    \begin{center}
        \Large
        \alert{能不能不 fine-tune，就预测副作用？}
    \end{center}
    \vfill
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}{数据怎么来：四步}
    \centering
    \begin{tikzpicture}[
        box/.style={draw, rounded corners=2pt, align=center,
                    minimum height=10mm, text width=30mm, font=\small},
        arr/.style={-Latex, thick, mDarkTeal},
    ]
        \node[box, fill=mDarkTeal!12] (c) {WikiText-103\\聚成 \textbf{10 簇}};
        \node[box, fill=mDarkTeal!12, right=6mm of c] (t) {每簇 1 个\\ forget set};
        \node[box, fill=mLightGreen!20, right=6mm of t] (u) {\textbf{10 次}\\ unlearn};
        \node[box, fill=mLightBrown!25, right=6mm of u] (m) {\textbf{10×10}\\ cross-ppl};

        \draw[arr] (c) -- (t);
        \draw[arr] (t) -- (u);
        \draw[arr] (u) -- (m);
    \end{tikzpicture}

    \vspace{1.4em}
    \centering
    \large
    每个 ckpt 在每个簇上都评 PPL $\Rightarrow$ 10×10 矩阵
\end{frame}

\begin{frame}{L1 / L2 / L3 在 10×10 矩阵里的位置}
    \centering
    \begin{tikzpicture}[font=\footnotesize]
        \def\s{0.38}
        \foreach \i in {0,...,9} {
            \foreach \j in {0,...,9} {
                \ifnum\i=\j
                    \fill[mLightBrown!60] (\j*\s, -\i*\s) rectangle +(\s,\s);
                \else
                    \fill[mDarkTeal!15] (\j*\s, -\i*\s) rectangle +(\s,\s);
                \fi
                \draw[white, thin] (\j*\s, -\i*\s) rectangle +(\s,\s);
            }
        }
        \node[above] at (10*\s/2, 0.1) {评测簇 $e$ →};
        \node[rotate=90, above] at (-0.18, -10*\s/2) {unlearn 簇 $m$ →};

        \node[right, align=left, font=\small] at (10*\s+0.7, -0.8)
            {\textbf{对角线}（橙）\\
             $m = e$\\
             \textbf{L1} = train\\
             \textbf{L2} = val / test};
        \draw[-Latex, thick, mLightBrown] (10*\s+0.7, -0.8) -- (3.0*\s+\s/2, -3.0*\s+\s/2);

        \node[right, align=left, font=\small] at (10*\s+0.7, -3.0)
            {\textbf{非对角线}（灰）\\
             $m \neq e$\\
             \textbf{L3} = test};
        \draw[-Latex, thick, mDarkTeal] (10*\s+0.7, -3.0) -- (7*\s+\s/2, -3*\s+\s/2);
    \end{tikzpicture}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{第一幕 · 三层真的存在}

\begin{frame}{这一幕要说服你三件事}
    \vspace{0.8em}
    \begin{description}
        \setlength\itemsep{0.8em}
        \item[\alert{C1}] 三层\textbf{严格递减}（L1 > L2 > L3），不是乱分箱。
        \item[\alert{C2}] L3 \textbf{不是 0}：无关样本也真被波及。
        \item[\alert{C3}] profile \textbf{随 forget set 变化}，所以\emph{值得审计}。
    \end{description}
\end{frame}

\begin{frame}{C1 + C2：headline 数字}
    \vspace{0.2em}
    \centering
    \small
    $r = \dfrac{\text{PPL}_{\text{unlearned}}}{\text{PPL}_{\text{base}}}$ \;（PPL 被抬高几倍）\quad
    geo-mean $r = \exp(\overline{\log r})$

    \vspace{0.6em}
    \begin{tabular}{@{}lccc@{}}
        \toprule
        & L1 forget & L2 locality & L3 spillover \\
        \midrule
        geo-mean $r$           & \textbf{1.76×} & \textbf{1.29×} & \textbf{1.16×} \\
        被抬高（$>1.1×$）占比    & 95.6\,\%       & 82.8\,\%       & \alert{61.6\,\%} \\
        \bottomrule
    \end{tabular}

    \vspace{1.2em}
    \begin{center}
        \large
        \textbf{C1 成立}：1.76 $>$ 1.29 $>$ 1.16\\[0.4em]
        \textbf{C2 成立}：L3 仍有 \alert{61.6\,\%} 样本被抬高
    \end{center}
\end{frame}

\begin{frame}{C1 + C2：一图}
    \centering
    \includegraphics[width=0.82\textwidth]{fig_three_layer_decay.pdf}

    \vspace{0.3em}
    \footnotesize
    左轴：L1 $>$ L2 $>$ L3\quad|\quad右轴：L3 仍有 \alert{61.6\,\%} 被抬高。
\end{frame}

\begin{frame}{C3：换个 forget set，副作用差多少？}
    \centering
    \small
    \begin{tabular}{@{}lccc@{}}
        \toprule
        & L1 & L2 & L3 \\
        \midrule
        最温和（\texttt{star}）    & 1.42 & 1.12 & 1.07 \\
        \alert{最严重（\texttt{storm}）} & \alert{\textbf{2.44}} & \alert{\textbf{1.78}} & \alert{\textbf{1.25}} \\
        \midrule
        极差比                    & \textbf{1.72×} & 1.60× & 1.17× \\
        \bottomrule
    \end{tabular}

    \vspace{1.2em}
    \begin{center}
        \Large
        同一算法，换 forget set，L1 副作用差 \alert{\textbf{1.7×}}。
    \end{center}

    \vspace{0.4em}
    \begin{center}
        \normalsize
        $\Rightarrow$ profile 是 forget set 的函数，值得\emph{审计}。
    \end{center}
\end{frame}

\begin{frame}{C3：一图}
    \centering
    \includegraphics[width=0.88\textwidth]{fig_per_forget_profile.pdf}

    \vspace{0.3em}
    \footnotesize
    按 L1 升序；\alert{storm} 三层同时最严重 $\Rightarrow$ 稳健坏 forget set。
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{第二幕 · 先审计，再决定}

\begin{frame}{审计问题：输入是什么、输出是什么}
    \vspace{0.4em}
    \begin{center}
        \begin{tikzpicture}[font=\small, node distance=10mm]
            \node[draw, rounded corners, fill=mDarkTeal!12, minimum width=32mm, minimum height=12mm, align=center] (in)
                {\textbf{输入}\\ forget set 的\\ 12 维几何特征};
            \node[draw, rounded corners, fill=mLightGreen!25, right=16mm of in, minimum width=28mm, minimum height=12mm, align=center] (box) {\textbf{审计器}\\ Ridge};
            \node[draw, rounded corners, fill=mLightBrown!25, right=16mm of box, minimum width=32mm, minimum height=12mm, align=center] (out)
                {\textbf{输出}\\ L1 / L2 / L3\\ 三个风险分数};
            \draw[-Latex, thick] (in) -- (box);
            \draw[-Latex, thick] (box) -- (out);
        \end{tikzpicture}
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \large
        \alert{不看 evaluation · 不跑 unlearn}
    \end{center}

    \vspace{0.6em}
    \footnotesize\centering
    12 维 = 方差 / 两两相似度 / centroid / 集中度（effective rank, isotropy）。\\
    10 forget set 上 Leave-One-Out。
\end{frame}

\begin{frame}{12 维几何特征是什么}
    \centering
    \footnotesize
    \begin{tabular}{@{}lll@{}}
        \toprule
        类别 & 特征 (共 12) & 回答什么 \\
        \midrule
        \textbf{分散度} & \texttt{emb\_variance\_mean / max}
            & 50 条文本在嵌入空间里\\
        & \texttt{pairwise\_eucl\_mean} & 铺得多开？ \\
        \midrule
        \textbf{相似度} & \texttt{pairwise\_sim\_mean / std / q90}
            & 文本两两之间\\
        & & 多相似？有多集中？ \\
        \midrule
        \textbf{位置 / 规模} & \texttt{centroid\_norm}
            & forget set 在空间里\\
        & \texttt{emb\_norm\_mean / std} & 离原点多远？ \\
        \midrule
        \textbf{形状 / 集中度} & \texttt{effective\_rank}
            & 这团点云是"一根线"\\
        & \texttt{isotropy} & 还是"一个球"？ \\
        & \texttt{spread\_over\_centroid} & \\
        \bottomrule
    \end{tabular}

    \vspace{0.8em}
    \begin{center}
        \normalsize
        \textbf{全部只看 forget set 自己的 50 条 embedding}\\
        不看任何 evaluation 文本、不跑 unlearn。
    \end{center}
\end{frame}

\begin{frame}{审计结果}
    \centering
    \begin{tabular}{@{}lccc@{}}
        \toprule
        & L1 forget & L2 locality & L3 spillover \\
        \midrule
        $R^2$ (audit / baseline $-0.24$) & \textbf{+0.44} & \textbf{+0.41} & +0.19 \\
        MAE (audit / baseline)       & \textbf{0.19} / 0.27 & \textbf{0.12} / 0.15 & \textbf{0.05} / 0.06 \\
        排序 Spearman $\rho$ & +0.62 & +0.62 & +0.61 \\
        95\% CI ($n_\text{boot}=10$k) & [$-0.22$, $+0.93$] & [$+0.05$, $+0.90$] & [$-0.01$, $+0.96$] \\
        Top-1 命中       & \alert{\checkmark \, storm} & $\times$ & $\times$ \\
        \bottomrule
    \end{tabular}

    \vspace{0.7em}
    \begin{center}
        \Large
        三层 $R^2$ 全正、MAE 全线胜 LOO-mean baseline
    \end{center}
    \vspace{0.2em}
    \begin{center}
        \normalsize
        但 $n=10$ CI 宽、L1 / L3 下界跨 0 $\Rightarrow$ \alert{排序级粗筛信号}，$n=100$ 收紧。
    \end{center}

    \vspace{0.2em}
    \footnotesize\centering\itshape
    Held-out: LOO over 10 clusters, Ridge $\alpha=1$, 12-d forget geometry.
\end{frame}

\begin{frame}{审计器的定位：预警器，不是替代品}
    \centering
    \small
    \begin{tabular}{@{}lll@{}}
        \toprule
         & \textbf{能做} & \textbf{不能做} \\
        \midrule
        \textbf{排序} & 谁比谁严重 ($\rho \approx 0.62$) & 绝对数值 \\
        \textbf{粗筛} & 红 / 黄 / 绿三档预警 & 给 unlearn 打分 \\
        \textbf{省算力} & 1 秒 vs. 数 GPU-hour & 省掉最终 unlearn \\
        \bottomrule
    \end{tabular}

    \vspace{1.0em}
    \begin{center}
        \Large
        \alert{审计器 = 便宜的粗筛预警器}
    \end{center}

    \vspace{0.4em}
    \begin{center}
        \normalsize
        用法：100 个候选 $\to$ 审计排序 $\to$ 真跑 top-$k$ 验证\\
        最终效果仍以真跑 unlearn 为准。
    \end{center}
\end{frame}

\begin{frame}{审计散点：预测 vs 真实}
    \centering
    \includegraphics[width=0.96\textwidth]{fig_audit_scatter.pdf}

    \vspace{0.2em}
    \footnotesize
    虚线 $y=x$；三层 $\rho \approx 0.62$；L3 幅度小、排序仍可用。
\end{frame}

\begin{frame}{几何 vs 文本表面}
    \centering
    \begin{tabular}{@{}lcc@{}}
        \toprule
        特征 & 维度 & $R^2$ \\
        \midrule
        文本表面统计        & 261          & +0.14 \\
        \textbf{纯几何}     & \textbf{16}  & \textbf{+0.36} \\
        \bottomrule
    \end{tabular}

    \vspace{1.2em}
    \begin{center}
        \Large
        特征少 \alert{16×}，$R^2$ 高 \alert{2.5×}
    \end{center}

    \vspace{0.4em}
    \begin{center}
        \normalsize
        corruption 更像\emph{几何}现象，而非文本难度现象。
    \end{center}
\end{frame}

\begin{frame}{诚实负结果：球覆盖不 work}
    \vspace{0.5em}
    \begin{center}
        \large
        试过：用 forget set 的半径圈 retain，看落入比例。
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \large
        结果：与真实 L3 的相关 $\rho = \alert{-0.42}$（\textbf{方向反了}）
    \end{center}

    \vspace{0.8em}
    \begin{center}
        \normalsize
        原因：簇之间互相重叠，球太大 $\Rightarrow$ 几乎全覆盖。\\[0.3em]
        改进：带符号投影 / mutual-reachability 距离。
    \end{center}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\section{讨论}

\begin{frame}{Related Work}
    \begin{center}
        \large
        相邻工作与我们的差别
    \end{center}

    \vspace{0.6em}
    \small
    \begin{tabular}{@{}p{0.34\textwidth}p{0.58\textwidth}@{}}
        \toprule
        相邻方向 & 我们的差别 \\
        \midrule
        Data-first 鲁棒性\newline (Dang+ ACL'24) & 训练集特征 $\to$ 对抗 ASR \newline\; $\Rightarrow$ 迁到 \emph{unlearning corruption} \\
        LLM unlearning 算法 & 我们固定 unlearner，分析\emph{数据依赖}的副作用 \\
        Influence / editing\newline locality & model-centric $\to$ \emph{data-centric} 三层风险 \\
        \bottomrule
    \end{tabular}
\end{frame}

\begin{frame}{Limitations}
    \vspace{0.3em}
    \begin{center}
        \large
        四个「单」
    \end{center}

    \vspace{0.6em}
    \begin{itemize}
        \setlength\itemsep{0.6em}
        \item 单 base model（Llama-3.1-8B-Instruct）
        \item 单 unlearner（GradAscent）
        \item 单 $n = 10$ forget sets（CI 较宽）
        \item 单指标 PPL（$\neq$ QA 准确率）
    \end{itemize}
\end{frame}

\begin{frame}{Future Work}
    \vspace{0.3em}
    \begin{center}
        \large
        对应四个「单」的解法
    \end{center}

    \vspace{0.6em}
    \begin{enumerate}
        \setlength\itemsep{0.5em}
        \item $n: 10 \to 100$（100 triplet 已生成）
        \item 加 NPO / GradDiff 验证审计器稳健
        \item PPL $\to$ QA accuracy（\texttt{2.extract-qa/} 已就位）
        \item \emph{audit-driven} forget 选择：挑预测 L3 最低的 $\mathcal{D}_f$
    \end{enumerate}
\end{frame}

\begin{frame}{论文定位}
    \begin{center}
        \large
        \emph{From Three-Layer Corruption to Forget-Set Audit\\ in LLM Unlearning.}
    \end{center}

    \vspace{0.8em}
    \textbf{三个卖点}
    \begin{enumerate}
        \item 三层 view + L3 61.6\,\% $\Rightarrow$ spillover \emph{不可忽略}
        \item benchmarking $\to$ \textbf{forget-set audit} 视角转换
        \item 仅几何就能排序预测（$\rho \approx 0.62$）
    \end{enumerate}

    \vspace{0.6em}
    \begin{center}
        \small
        \textbf{升档}：$n$=10 $\to$ 100 + 第二个 unlearner $\Rightarrow$ NeurIPS / ICML
    \end{center}
\end{frame}

% ────────────────────────────────────────────────────────────────────────────
\begin{frame}[standout]
    三层衰减，但不归零。\\[0.5em]
    \normalsize
    \textbf{1.76× → 1.29× → 1.16×}\\[0.3em]
    审计 $\rho \approx 0.62$，无需 unlearn。
\end{frame}

\appendix

\begin{frame}[fragile]{附录 — 复现主结果}
    \footnotesize
\begin{verbatim}
# 0. Unlearn 与 cross-ppl（已完成可跳过）
#    ⇒ 2.extract-ppl/wikitext_cross_metrics_detail.json

# 1. 第一幕：三层 ground truth
cd 2.extract-ppl
python analyze_corruption.py          # ⇒ corruption_summary.json

# 2. 第二幕 A：per-sample 几何预测器
cd ../4.regression-predictor
python 3.corruption_from_geometry.py  # ⇒ geometry/geometry_results.json

# 3. 第二幕 B：forget-set 审计（LOO, Part 1+2+3+3b）
python 4.audit_experiments.py         # ⇒ audit/audit_summary.json
\end{verbatim}
\end{frame}

\begin{frame}{附录 — 关键 artefact}
    \footnotesize
    \begin{tabular}{@{}ll@{}}
        \toprule
        产物 & 路径 \\
        \midrule
        三层 corruption       & \texttt{2.extract-ppl/corruption\_summary.json} \\
        审计 LOO 预测          & \texttt{4.regression-predictor/audit/audit\_summary.json} \\
        每 forget set profile  & \texttt{audit/part1\_corruption\_profile.csv} \\
        审计特征               & \texttt{audit/part2\_forget\_features.csv} \\
        \bottomrule
    \end{tabular}
\end{frame}

\end{document}
